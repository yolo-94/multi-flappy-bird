<!DOCTYPE html>
html(lang="fr")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Multi Flappy Bird
        != entrypoints.css("default")
    body
        div#game-zone
        != entrypoints.js("default")